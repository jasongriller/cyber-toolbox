[build-system]
# setuptools floor is 83.0.0: PYSEC-2026-3447 affects every release below it.
requires = ["setuptools>=83.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "stig-parser"
version = "0.1.0"
description = "Parse XCCDF compliance scan results and produce consolidated STIG findings in Excel"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0,<4.0",
    # lxml floor is 6.1.0: PYSEC-2026-87 affects every release below it.
    "lxml>=6.1.0,<7.0",
    "openpyxl>=3.1,<4.0",
    "boto3>=1.34,<2.0",
]

[project.scripts]
stig-parser = "app.cli:main"

[project.optional-dependencies]
dev = [
    # pytest floor is 9.0.3: PYSEC-2026-1845 affects every release below it.
    "pytest>=9.0.3,<10.0",
    "pytest-cov>=4.0,<8.0",
    "moto[s3,dynamodb]>=5.0,<6.0",
    "pip-audit>=2.7",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
